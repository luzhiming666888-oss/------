# 2026-06-23 工作日志

## 《校园选课记录检索与大数据分析系统》项目骨架创建

**任务**：按要求在 `E:\数据结构实训` 下创建 C 语言项目头文件与源文件骨架。

**创建文件清单**：
- `include/record.h` — CourseRecord 结构体 + 辅助函数声明
- `include/avl.h`    — AVL 树节点 + 4 种旋转 + 完整接口声明
- `include/hash.h`   — 拉链法哈希表 + DJB2/FNV-1a + 接口声明
- `include/util.h`   — JSON I/O + 排序(SortKey枚举) + 筛选(FilterCriteria) + 统计接口
- `src/main.c`       — CLI 菜单框架（含双索引初始化、加载、保存）
- `src/avl.c`        — AVL 树模板（旋转/插入/删除/查找 TODO 占位）
- `src/hash.c`       — 哈希表模板（DJB2/FNV-1a 已实现，其余 TODO）
- `src/util.c`       — 工具模板（record_make_key/record_print/util_trim 已实现，其余 TODO）

**技术约束确认**：纯 C，禁用 C++ STL，唯一主键 `学号_课程号`，JSON Lines 格式，手写序列化。

## 《校园选课记录检索与大数据分析系统》报告骨架生成

**任务**：按长沙学院课程设计说明书规范生成 `report_draft.md`。

**文件位置**：`E:\数据结构实训\report_draft.md`

**章节完整性**：
- 第 1 章 问题分析（4 节，含开发环境表格）
- 第 2 章 数据结构设计（3 节，含 CourseRecord/AVLNode/HashTable 完整 C 代码定义与 ADT 操作表）
- 第 3 章 方案对比与选型论证（4 节，含多维度对比表格与复杂度分析表）
- 第 4 章 实现说明（3 节，含模块职责表、核心函数说明与代码片段占位）
- 第 5 章 测试与验证（3 节，含 16 条功能测试用例表 + 3 张性能对比空白表格）
- 第 6 章 总结（3 节）
- 参考文献 + 附录 A/B/C
- 每节均有【撰写说明】指引块，完稿后需删除
